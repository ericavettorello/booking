# Анализ файлов проекта

## ✅ Обязательные файлы (для работы системы)

### Основные компоненты:
- **`pg_driver.py`** - Драйвер для работы с PostgreSQL (необходим)
- **`backend.py`** - Инициализация и создание таблиц (необходим)
- **`requirements.txt`** - Зависимости проекта (необходим)
- **`.env`** - Настройки подключения к БД (необходим, не в git)

### Модели (папка `models/`):
- **`models/user.py`** - Модель пользователя (необходима)
- **`models/table.py`** - Модель стола (необходима)
- **`models/booking.py`** - Модель бронирования (необходима)

---

## 📋 Вспомогательные файлы (полезны, но не обязательны)

### Утилиты для проверки:
- **`check_tables.py`** - Проверка наличия таблиц (полезен для отладки)
- **`connect_db.py`** - Проверка подключения к БД (полезен для отладки)
- **`run.ps1`** - Скрипт для удобного запуска (полезен для упрощения работы)
- **`setup_env.ps1`** - Скрипт первоначальной настройки (полезен при развертывании)

### Документация:
- **`README.md`** - Документация проекта (рекомендуется оставить)

---

## ❌ Неиспользуемые файлы (можно удалить)

### Не используются в системе бронирования:
- **`example_usage.py`** - Примеры использования драйвера (не нужен для системы бронирования)
- **`main.py`** - Пустой файл (не используется)

### Не используются в коде:
- **`repositories/`** - Папка с репозиториями (не используется, вы сказали что репозиторий не нужен)
  - `repositories/__init__.py`
  - `repositories/user_repository.py`

---

## 🎯 Минимальный набор для работы

Для работы системы бронирования **обязательно** нужны:

```
BOOKING/
├── pg_driver.py            ✅ Обязателен
├── backend.py              ✅ Обязателен
├── requirements.txt        ✅ Обязателен
├── .env                    ✅ Обязателен (создается вручную)
└── models/
    ├── user.py            ✅ Обязателен
    ├── table.py           ✅ Обязателен
    └── booking.py         ✅ Обязателен
```

**Всего: 6 обязательных файлов**

---

## 💡 Рекомендация

**Оставить:**
- Все обязательные файлы
- Вспомогательные скрипты (`check_tables.py`, `connect_db.py`, `run.ps1`, `setup_env.ps1`)
- `README.md` для документации

**Удалить:**
- `example_usage.py` (примеры не нужны для работы системы)
- `main.py` (пустой файл)
- Папку `repositories/` (не используется в проекте)

