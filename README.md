# Система бронирования столов в ресторане

GUI-приложение для управления бронированием столов в ресторане на Python с tkinter и PostgreSQL.

## 🎯 Основные возможности

- **Управление пользователями** - создание, редактирование, удаление клиентов и администраторов
- **Управление столами** - добавление столов с указанием количества мест и статуса (available/unavailable)
- **Бронирование столов** - создание бронирований с автоматической проверкой доступности
- **Графический интерфейс** - удобный GUI на tkinter для всех операций
- **База данных** - автоматическая инициализация таблиц PostgreSQL

## 🛠 Технологии

- Python 3.x
- tkinter - графический интерфейс
- PostgreSQL - база данных
- psycopg2 - драйвер для PostgreSQL
- bcrypt - хеширование паролей

## 📋 Содержание

- [Установка и настройка](#-установка-и-настройка)
- [Подключение к базе данных](#-подключение-к-базе-данных)
- [Создание таблиц](#-создание-таблиц)
- [Запуск GUI](#-запуск-gui)
- [Использование](#-использование)
- [Структура проекта](#-структура-проекта)

---

## 🚀 Установка и настройка

### 1. Создание виртуального окружения

```powershell
python -m venv venv
```

### 2. Активация виртуального окружения

**В PowerShell:**
```powershell
.\venv\Scripts\Activate.ps1
```

**В обычной командной строке:**
```cmd
venv\Scripts\activate
```

### 3. Установка зависимостей

```powershell
pip install -r requirements.txt
```

### 4. Настройка подключения к базе данных

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=booking
DB_USER=postgres
DB_PASSWORD=ваш_пароль
```

**Важно:** 
- База данных `booking` должна быть создана в PostgreSQL
- Если база данных не существует, создайте её командой:
  ```sql
  CREATE DATABASE booking;
  ```

---

## 🔌 Подключение к базе данных

### Способ 1: Через скрипт run.ps1 (рекомендуется, самый простой)

**Проверка подключения и просмотр таблиц:**
```powershell
.\run.ps1 connect_db.py
```

**Проверка таблиц:**
```powershell
.\run.ps1 check_tables.py
```

**Создание таблиц:**
```powershell
.\run.ps1 backend.py
```

**Преимущество:** Скрипт автоматически активирует виртуальное окружение и устанавливает правильную кодировку.

### Способ 2: Вручную с активацией venv

**Проверка подключения и просмотр таблиц:**
```powershell
.\venv\Scripts\Activate.ps1
python connect_db.py
```

**Или проверка таблиц:**
```powershell
.\venv\Scripts\Activate.ps1
python check_tables.py
```

**Важно:** Не забудьте активировать виртуальное окружение перед запуском!

### Способ 3: Через psql (PostgreSQL клиент)

**Настройка кодировки (для Windows PowerShell):**
```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
```

**Подключение к базе данных:**
```powershell
& "C:\Program Files\PostgreSQL\18\bin\psql.exe" -h localhost -p 5432 -U postgres -d booking
```

**Если psql не найден, используйте полный путь:**
```powershell
& "C:\Program Files\PostgreSQL\18\bin\psql.exe" -U postgres -d booking
```

Система запросит пароль пользователя `postgres`.

**Альтернатива с паролем через переменную окружения:**
```powershell
$env:PGPASSWORD="ваш_пароль"
& "C:\Program Files\PostgreSQL\18\bin\psql.exe" -h localhost -p 5432 -U postgres -d booking
```

### Способ 4: Через pgAdmin 4

1. Откройте pgAdmin 4
2. Подключитесь к серверу PostgreSQL
3. Раскройте: `Servers` → `PostgreSQL 18` → `Базы данных` → `booking`
4. Раскройте: `Схемы` → `public` → **`Таблицы`**
5. Вы увидите созданные таблицы: `users`, `tables`, `bookings`

---

## 🗄️ Создание таблиц

**Самый простой способ (рекомендуется):**
```powershell
.\run.ps1 backend.py
```

**Или вручную:**
```powershell
.\venv\Scripts\Activate.ps1
python backend.py
```

После выполнения вы увидите:
```
Инициализация базы данных...
==================================================
Создание таблицы users...
✓ Таблица users создана успешно
Создание таблицы tables...
✓ Таблица tables создана успешно
Создание таблицы bookings...
✓ Таблица bookings создана успешно (с внешними ключами)

Все таблицы созданы!
```

**Примечание:** Таблицы создаются с условием `IF NOT EXISTS`, поэтому повторный запуск безопасен.

---

## 🖥️ Запуск GUI

### Быстрый запуск (рекомендуется)

**Windows PowerShell:**
```powershell
.\run_gui.ps1
```

**Или через run.ps1:**
```powershell
.\run.ps1 main.py
```

**Или вручную:**
```powershell
.\venv\Scripts\Activate.ps1
python main.py
```

### Возможности GUI

- **Вкладка "Пользователи"**: Управление пользователями с ролями (client/admin)
- **Вкладка "Столы"**: Управление столами с фильтрацией по номеру и количеству мест
- **Вкладка "Бронирования"**: Создание бронирований с автоматической проверкой доступности столов
- **Вкладка "База данных"**: Инициализация и управление таблицами БД

**Особенности:**
- Автоматическая проверка доступности столов при бронировании
- Защита от бронирования недоступных (unavailable) столов
- Фильтрация и поиск по всем сущностям
- Автоматическое заполнение формы при выборе записи в таблице

---

## 💻 Использование

### Пример работы с моделями (через Python API)

```python
from pg_driver import PostgreSQLDriver
from models.user import UserModel
from models.table import TableModel
from models.booking import BookingModel

# Инициализация драйвера
with PostgreSQLDriver() as db:
    # Работа с пользователями
    user_model = UserModel(db)
    user_id = user_model.create_user(
        name="Иван Иванов",
        email="ivan@example.com",
        password="пароль123"
    )
    
    # Работа со столами
    table_model = TableModel(db)
    table_id = table_model.create_table_item(
        table_number=5,
        seats=4,
        status="available"
    )
    
    # Работа с бронированиями
    booking_model = BookingModel(db)
    from datetime import date, time
    
    booking_id = booking_model.create_booking(
        user_id=user_id,
        table_id=table_id,
        booking_date=date(2024, 12, 25),
        booking_time=time(19, 0)
    )
```

### Проверка подключения

```python
from pg_driver import PostgreSQLDriver

with PostgreSQLDriver() as db:
    # Проверка подключения
    tables = db.execute_raw(
        "SELECT table_name FROM information_schema.tables WHERE table_schema = 'public'"
    )
    print(f"Найдено таблиц: {len(tables)}")
```

---

## 📁 Структура проекта

```
BOOKING/
├── main.py                 # Точка входа для GUI приложения
├── gui.py                  # Графический интерфейс (tkinter)
├── backend.py              # Инициализация и создание таблиц
├── pg_driver.py            # Драйвер для работы с PostgreSQL
├── connect_db.py           # Скрипт для проверки подключения
├── check_tables.py         # Скрипт для проверки таблиц
├── example_usage.py        # Примеры использования API
├── models/
│   ├── user.py            # Модель пользователя
│   ├── table.py           # Модель стола
│   └── booking.py         # Модель бронирования
├── requirements.txt        # Зависимости проекта
├── run.ps1                 # Скрипт запуска (PowerShell)
├── run_gui.ps1            # Скрипт запуска GUI (PowerShell)
├── setup_env.ps1          # Скрипт настройки окружения
├── .env                    # Настройки подключения (не в git, создать вручную)
└── README.md              # Этот файл
```

---

## 🔧 Решение проблем

### Ошибка: "ModuleNotFoundError: No module named 'pg_driver'"

**Решение:** Убедитесь, что виртуальное окружение активировано:
```powershell
.\venv\Scripts\Activate.ps1
```

### Ошибка: "FATAL: password authentication failed"

**Решение:** 
1. Проверьте правильность пароля в файле `.env`
2. Убедитесь, что пользователь `postgres` существует в PostgreSQL

### Ошибка: "FATAL: database 'booking' does not exist"

**Решение:** Создайте базу данных:
```sql
CREATE DATABASE booking;
```

### Ошибка с кодировкой в PowerShell

**Решение:** Установите кодировку UTF-8:
```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$env:PYTHONIOENCODING = "utf-8"
```

### Таблицы не видны в pgAdmin

**Решение:** 
1. Раскройте узел "Таблицы" в pgAdmin (кликните по стрелке)
2. Обновите представление (F5)
3. Проверьте подключение к правильной базе данных

---

## 📝 Полезные команды

### Просмотр списка баз данных
```powershell
& "C:\Program Files\PostgreSQL\18\bin\psql.exe" -U postgres -l
```

### Подключение к базе данных
```powershell
& "C:\Program Files\PostgreSQL\18\bin\psql.exe" -U postgres -d booking
```

### Проверка таблиц через Python (с run.ps1)
```powershell
.\run.ps1 check_tables.py
```

### Проверка подключения (с run.ps1)
```powershell
.\run.ps1 connect_db.py
```

### Создание таблиц (с run.ps1)
```powershell
.\run.ps1 backend.py
```

### Запуск GUI (с run.ps1)
```powershell
.\run.ps1 main.py
```

**Или используйте специализированный скрипт:**
```powershell
.\run_gui.ps1
```

---

## 📋 Функционал

- ✅ CRUD операции для пользователей, столов и бронирований
- ✅ Проверка доступности столов перед бронированием
- ✅ Фильтрация и поиск по всем сущностям
- ✅ Автоматическое управление статусами
- ✅ Защита от бронирования недоступных столов
- ✅ Графический интерфейс для всех операций

---

## 📄 Лицензия

Этот проект создан в образовательных целях.
